# Security policy

Do not open public issues with real bank data, screenshots of full transaction ledgers, account numbers, card numbers, or raw DBS CSV rows.

If you find a security or privacy problem, report the behavior without including private data. A minimal synthetic CSV row is enough.

This app has no backend by default. Most privacy risk comes from accidental data exposure: committed CSV files, screenshots, browser extensions, or forks that add network calls.
